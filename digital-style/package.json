{
  "name": "digital-style",
  "version": "1.0.0",
  "description": "digital style library",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && node-sass -i src/styles.scss -o dist",
    "watch": "npm run build && node-sass src/styles.scss -wo dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
